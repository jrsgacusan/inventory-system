import styled from 'styled-components';

const S = {
  LoginForm: styled.form`
    display: flex;
    flex-direction: column;
    width: 100%;
    text-align: center;
    row-gap: 10px;
  `,
};

export default S;
